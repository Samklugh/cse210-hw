using System;

class Program
{
    // Exceeding requirements: Added a simple "level" title that updates as score increases.
    static void Main(string[] args)
    {
        GoalManager manager = new GoalManager();
        manager.Start();
    }
}
